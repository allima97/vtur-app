---
import "../../styles/global.css";
import "../../styles/auth-extra.css";
import BrandHead from "../../components/BrandHead.astro";
import AuthConviteIsland from "../../components/islands/AuthConviteIsland.tsx";
import { SYSTEM_NAME } from "../../lib/systemName";

const pageTitle = `Convite - ${SYSTEM_NAME}`;
---

<html lang="pt-BR">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <BrandHead title={pageTitle} canonicalPath={Astro.url.pathname} noindex={true} />
    <title>{pageTitle}</title>
  </head>
  <body class="auth-body">
    <AuthConviteIsland client:visible />
  </body>
</html>
