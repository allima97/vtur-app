---
import {
  APPLE_TOUCH_ICON_PATH,
  APP_URL,
  BRAND_ICON_PATH,
  BRAND_OG_IMAGE_PATH,
  MANIFEST_PATH,
  SYSTEM_DESCRIPTION,
  SYSTEM_NAME,
  SYSTEM_TITLE,
} from "../lib/systemName";
import {
  buildPrimeThemeHref,
  PRIME_DEFAULT_THEME,
  PRIME_THEME_LINK_ID,
  PRIME_THEME_OPTIONS,
  PRIME_THEME_STORAGE_KEY,
} from "../lib/primeTheme";

type Props = {
  title?: string;
  description?: string;
  canonicalPath?: string;
  noindex?: boolean;
};

const {
  title = SYSTEM_TITLE,
  description = SYSTEM_DESCRIPTION,
  canonicalPath = "/",
  noindex = false,
} = Astro.props as Props;

const canonicalUrl = new URL(canonicalPath, APP_URL).toString();
const ogImageUrl = new URL(BRAND_OG_IMAGE_PATH, APP_URL).toString();
const defaultPrimeThemeHref = buildPrimeThemeHref(PRIME_DEFAULT_THEME);
const allowedPrimeThemes = PRIME_THEME_OPTIONS.map((option) => option.name);
const bootstrapThemeScript = `(function(){try{var storageKey=${JSON.stringify(PRIME_THEME_STORAGE_KEY)};var defaultTheme=${JSON.stringify(PRIME_DEFAULT_THEME)};var allowed=${JSON.stringify(allowedPrimeThemes)};var theme=(window.localStorage.getItem(storageKey)||"").trim().toLowerCase();if(!/^[a-z0-9-]+$/i.test(theme)||allowed.indexOf(theme)===-1){theme=defaultTheme;}var link=document.getElementById(${JSON.stringify(PRIME_THEME_LINK_ID)});if(link){var nextHref="/themes/"+theme+"/theme.css";if(link.getAttribute("href")!==nextHref){link.setAttribute("href",nextHref);}}}catch(_err){}})();`;
---

<meta name="theme-color" content="#0b0f2a" />
<meta name="application-name" content={SYSTEM_NAME} />
<meta name="apple-mobile-web-app-title" content={SYSTEM_NAME} />
<meta name="apple-mobile-web-app-capable" content="yes" />
<meta name="mobile-web-app-capable" content="yes" />
<meta name="description" content={description} />
<meta property="og:site_name" content={SYSTEM_NAME} />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:type" content="website" />
<meta property="og:url" content={canonicalUrl} />
<meta property="og:image" content={ogImageUrl} />
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={title} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={ogImageUrl} />
<link rel="canonical" href={canonicalUrl} />
<link rel="manifest" href={MANIFEST_PATH} />
<link rel="icon" href="/favicon.ico" sizes="any" />
<link rel="icon" href="/favicon.png" type="image/png" sizes="64x64" />
<link rel="icon" type="image/png" sizes="192x192" href={BRAND_ICON_PATH} />
<link rel="apple-touch-icon" href={APPLE_TOUCH_ICON_PATH} />
<link id={PRIME_THEME_LINK_ID} rel="stylesheet" href={defaultPrimeThemeHref} />
<script is:inline set:html={bootstrapThemeScript}></script>
{noindex && <meta name="robots" content="noindex, nofollow" />}
